// Package problem implements problem management: problem records,
// root-cause investigation, known-error linkage and the
// open → investigating → identified → resolved → closed lifecycle.
package problem
