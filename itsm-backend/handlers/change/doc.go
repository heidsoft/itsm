// Package change implements the change management vertical slice:
// change tickets, approval chains, CI impact analysis and the
// state machine with CAS-based status transitions.
// New business logic must live in this package's service layer.
package change
