// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"itsm-backend/ent/predicate"
	"itsm-backend/ent/ticketassignmentrule"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TicketAssignmentRuleQuery is the builder for querying TicketAssignmentRule entities.
type TicketAssignmentRuleQuery struct {
	config
	ctx        *QueryContext
	order      []ticketassignmentrule.OrderOption
	inters     []Interceptor
	predicates []predicate.TicketAssignmentRule
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TicketAssignmentRuleQuery builder.
func (tarq *TicketAssignmentRuleQuery) Where(ps ...predicate.TicketAssignmentRule) *TicketAssignmentRuleQuery {
	tarq.predicates = append(tarq.predicates, ps...)
	return tarq
}

// Limit the number of records to be returned by this query.
func (tarq *TicketAssignmentRuleQuery) Limit(limit int) *TicketAssignmentRuleQuery {
	tarq.ctx.Limit = &limit
	return tarq
}

// Offset to start from.
func (tarq *TicketAssignmentRuleQuery) Offset(offset int) *TicketAssignmentRuleQuery {
	tarq.ctx.Offset = &offset
	return tarq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tarq *TicketAssignmentRuleQuery) Unique(unique bool) *TicketAssignmentRuleQuery {
	tarq.ctx.Unique = &unique
	return tarq
}

// Order specifies how the records should be ordered.
func (tarq *TicketAssignmentRuleQuery) Order(o ...ticketassignmentrule.OrderOption) *TicketAssignmentRuleQuery {
	tarq.order = append(tarq.order, o...)
	return tarq
}

// First returns the first TicketAssignmentRule entity from the query.
// Returns a *NotFoundError when no TicketAssignmentRule was found.
func (tarq *TicketAssignmentRuleQuery) First(ctx context.Context) (*TicketAssignmentRule, error) {
	nodes, err := tarq.Limit(1).All(setContextOp(ctx, tarq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ticketassignmentrule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tarq *TicketAssignmentRuleQuery) FirstX(ctx context.Context) *TicketAssignmentRule {
	node, err := tarq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TicketAssignmentRule ID from the query.
// Returns a *NotFoundError when no TicketAssignmentRule ID was found.
func (tarq *TicketAssignmentRuleQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tarq.Limit(1).IDs(setContextOp(ctx, tarq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ticketassignmentrule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tarq *TicketAssignmentRuleQuery) FirstIDX(ctx context.Context) int {
	id, err := tarq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TicketAssignmentRule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TicketAssignmentRule entity is found.
// Returns a *NotFoundError when no TicketAssignmentRule entities are found.
func (tarq *TicketAssignmentRuleQuery) Only(ctx context.Context) (*TicketAssignmentRule, error) {
	nodes, err := tarq.Limit(2).All(setContextOp(ctx, tarq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ticketassignmentrule.Label}
	default:
		return nil, &NotSingularError{ticketassignmentrule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tarq *TicketAssignmentRuleQuery) OnlyX(ctx context.Context) *TicketAssignmentRule {
	node, err := tarq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TicketAssignmentRule ID in the query.
// Returns a *NotSingularError when more than one TicketAssignmentRule ID is found.
// Returns a *NotFoundError when no entities are found.
func (tarq *TicketAssignmentRuleQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tarq.Limit(2).IDs(setContextOp(ctx, tarq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ticketassignmentrule.Label}
	default:
		err = &NotSingularError{ticketassignmentrule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tarq *TicketAssignmentRuleQuery) OnlyIDX(ctx context.Context) int {
	id, err := tarq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TicketAssignmentRules.
func (tarq *TicketAssignmentRuleQuery) All(ctx context.Context) ([]*TicketAssignmentRule, error) {
	ctx = setContextOp(ctx, tarq.ctx, ent.OpQueryAll)
	if err := tarq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TicketAssignmentRule, *TicketAssignmentRuleQuery]()
	return withInterceptors[[]*TicketAssignmentRule](ctx, tarq, qr, tarq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tarq *TicketAssignmentRuleQuery) AllX(ctx context.Context) []*TicketAssignmentRule {
	nodes, err := tarq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TicketAssignmentRule IDs.
func (tarq *TicketAssignmentRuleQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tarq.ctx.Unique == nil && tarq.path != nil {
		tarq.Unique(true)
	}
	ctx = setContextOp(ctx, tarq.ctx, ent.OpQueryIDs)
	if err = tarq.Select(ticketassignmentrule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tarq *TicketAssignmentRuleQuery) IDsX(ctx context.Context) []int {
	ids, err := tarq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tarq *TicketAssignmentRuleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tarq.ctx, ent.OpQueryCount)
	if err := tarq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tarq, querierCount[*TicketAssignmentRuleQuery](), tarq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tarq *TicketAssignmentRuleQuery) CountX(ctx context.Context) int {
	count, err := tarq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tarq *TicketAssignmentRuleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tarq.ctx, ent.OpQueryExist)
	switch _, err := tarq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tarq *TicketAssignmentRuleQuery) ExistX(ctx context.Context) bool {
	exist, err := tarq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TicketAssignmentRuleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tarq *TicketAssignmentRuleQuery) Clone() *TicketAssignmentRuleQuery {
	if tarq == nil {
		return nil
	}
	return &TicketAssignmentRuleQuery{
		config:     tarq.config,
		ctx:        tarq.ctx.Clone(),
		order:      append([]ticketassignmentrule.OrderOption{}, tarq.order...),
		inters:     append([]Interceptor{}, tarq.inters...),
		predicates: append([]predicate.TicketAssignmentRule{}, tarq.predicates...),
		// clone intermediate query.
		sql:  tarq.sql.Clone(),
		path: tarq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TicketAssignmentRule.Query().
//		GroupBy(ticketassignmentrule.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tarq *TicketAssignmentRuleQuery) GroupBy(field string, fields ...string) *TicketAssignmentRuleGroupBy {
	tarq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TicketAssignmentRuleGroupBy{build: tarq}
	grbuild.flds = &tarq.ctx.Fields
	grbuild.label = ticketassignmentrule.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.TicketAssignmentRule.Query().
//		Select(ticketassignmentrule.FieldName).
//		Scan(ctx, &v)
func (tarq *TicketAssignmentRuleQuery) Select(fields ...string) *TicketAssignmentRuleSelect {
	tarq.ctx.Fields = append(tarq.ctx.Fields, fields...)
	sbuild := &TicketAssignmentRuleSelect{TicketAssignmentRuleQuery: tarq}
	sbuild.label = ticketassignmentrule.Label
	sbuild.flds, sbuild.scan = &tarq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TicketAssignmentRuleSelect configured with the given aggregations.
func (tarq *TicketAssignmentRuleQuery) Aggregate(fns ...AggregateFunc) *TicketAssignmentRuleSelect {
	return tarq.Select().Aggregate(fns...)
}

func (tarq *TicketAssignmentRuleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tarq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tarq); err != nil {
				return err
			}
		}
	}
	for _, f := range tarq.ctx.Fields {
		if !ticketassignmentrule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tarq.path != nil {
		prev, err := tarq.path(ctx)
		if err != nil {
			return err
		}
		tarq.sql = prev
	}
	return nil
}

func (tarq *TicketAssignmentRuleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TicketAssignmentRule, error) {
	var (
		nodes = []*TicketAssignmentRule{}
		_spec = tarq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TicketAssignmentRule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TicketAssignmentRule{config: tarq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tarq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tarq *TicketAssignmentRuleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tarq.querySpec()
	_spec.Node.Columns = tarq.ctx.Fields
	if len(tarq.ctx.Fields) > 0 {
		_spec.Unique = tarq.ctx.Unique != nil && *tarq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tarq.driver, _spec)
}

func (tarq *TicketAssignmentRuleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ticketassignmentrule.Table, ticketassignmentrule.Columns, sqlgraph.NewFieldSpec(ticketassignmentrule.FieldID, field.TypeInt))
	_spec.From = tarq.sql
	if unique := tarq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tarq.path != nil {
		_spec.Unique = true
	}
	if fields := tarq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ticketassignmentrule.FieldID)
		for i := range fields {
			if fields[i] != ticketassignmentrule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tarq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tarq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tarq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tarq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tarq *TicketAssignmentRuleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tarq.driver.Dialect())
	t1 := builder.Table(ticketassignmentrule.Table)
	columns := tarq.ctx.Fields
	if len(columns) == 0 {
		columns = ticketassignmentrule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tarq.sql != nil {
		selector = tarq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tarq.ctx.Unique != nil && *tarq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tarq.predicates {
		p(selector)
	}
	for _, p := range tarq.order {
		p(selector)
	}
	if offset := tarq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tarq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TicketAssignmentRuleGroupBy is the group-by builder for TicketAssignmentRule entities.
type TicketAssignmentRuleGroupBy struct {
	selector
	build *TicketAssignmentRuleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (targb *TicketAssignmentRuleGroupBy) Aggregate(fns ...AggregateFunc) *TicketAssignmentRuleGroupBy {
	targb.fns = append(targb.fns, fns...)
	return targb
}

// Scan applies the selector query and scans the result into the given value.
func (targb *TicketAssignmentRuleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, targb.build.ctx, ent.OpQueryGroupBy)
	if err := targb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TicketAssignmentRuleQuery, *TicketAssignmentRuleGroupBy](ctx, targb.build, targb, targb.build.inters, v)
}

func (targb *TicketAssignmentRuleGroupBy) sqlScan(ctx context.Context, root *TicketAssignmentRuleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(targb.fns))
	for _, fn := range targb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*targb.flds)+len(targb.fns))
		for _, f := range *targb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*targb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := targb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TicketAssignmentRuleSelect is the builder for selecting fields of TicketAssignmentRule entities.
type TicketAssignmentRuleSelect struct {
	*TicketAssignmentRuleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tars *TicketAssignmentRuleSelect) Aggregate(fns ...AggregateFunc) *TicketAssignmentRuleSelect {
	tars.fns = append(tars.fns, fns...)
	return tars
}

// Scan applies the selector query and scans the result into the given value.
func (tars *TicketAssignmentRuleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tars.ctx, ent.OpQuerySelect)
	if err := tars.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TicketAssignmentRuleQuery, *TicketAssignmentRuleSelect](ctx, tars.TicketAssignmentRuleQuery, tars, tars.inters, v)
}

func (tars *TicketAssignmentRuleSelect) sqlScan(ctx context.Context, root *TicketAssignmentRuleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tars.fns))
	for _, fn := range tars.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tars.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tars.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
