// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"itsm-backend/ent/mspallocation"
	"itsm-backend/ent/predicate"
	"itsm-backend/ent/tenant"
	"itsm-backend/ent/user"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MSPAllocationQuery is the builder for querying MSPAllocation entities.
type MSPAllocationQuery struct {
	config
	ctx                *QueryContext
	order              []mspallocation.OrderOption
	inters             []Interceptor
	predicates         []predicate.MSPAllocation
	withMspUser        *UserQuery
	withCustomerTenant *TenantQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MSPAllocationQuery builder.
func (_q *MSPAllocationQuery) Where(ps ...predicate.MSPAllocation) *MSPAllocationQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *MSPAllocationQuery) Limit(limit int) *MSPAllocationQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *MSPAllocationQuery) Offset(offset int) *MSPAllocationQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *MSPAllocationQuery) Unique(unique bool) *MSPAllocationQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *MSPAllocationQuery) Order(o ...mspallocation.OrderOption) *MSPAllocationQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryMspUser chains the current query on the "msp_user" edge.
func (_q *MSPAllocationQuery) QueryMspUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mspallocation.Table, mspallocation.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, mspallocation.MspUserTable, mspallocation.MspUserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCustomerTenant chains the current query on the "customer_tenant" edge.
func (_q *MSPAllocationQuery) QueryCustomerTenant() *TenantQuery {
	query := (&TenantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mspallocation.Table, mspallocation.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, mspallocation.CustomerTenantTable, mspallocation.CustomerTenantPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MSPAllocation entity from the query.
// Returns a *NotFoundError when no MSPAllocation was found.
func (_q *MSPAllocationQuery) First(ctx context.Context) (*MSPAllocation, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mspallocation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *MSPAllocationQuery) FirstX(ctx context.Context) *MSPAllocation {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MSPAllocation ID from the query.
// Returns a *NotFoundError when no MSPAllocation ID was found.
func (_q *MSPAllocationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mspallocation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *MSPAllocationQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MSPAllocation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MSPAllocation entity is found.
// Returns a *NotFoundError when no MSPAllocation entities are found.
func (_q *MSPAllocationQuery) Only(ctx context.Context) (*MSPAllocation, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mspallocation.Label}
	default:
		return nil, &NotSingularError{mspallocation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *MSPAllocationQuery) OnlyX(ctx context.Context) *MSPAllocation {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MSPAllocation ID in the query.
// Returns a *NotSingularError when more than one MSPAllocation ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *MSPAllocationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mspallocation.Label}
	default:
		err = &NotSingularError{mspallocation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *MSPAllocationQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MSPAllocations.
func (_q *MSPAllocationQuery) All(ctx context.Context) ([]*MSPAllocation, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MSPAllocation, *MSPAllocationQuery]()
	return withInterceptors[[]*MSPAllocation](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *MSPAllocationQuery) AllX(ctx context.Context) []*MSPAllocation {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MSPAllocation IDs.
func (_q *MSPAllocationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(mspallocation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *MSPAllocationQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *MSPAllocationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*MSPAllocationQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *MSPAllocationQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *MSPAllocationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *MSPAllocationQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MSPAllocationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *MSPAllocationQuery) Clone() *MSPAllocationQuery {
	if _q == nil {
		return nil
	}
	return &MSPAllocationQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]mspallocation.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.MSPAllocation{}, _q.predicates...),
		withMspUser:        _q.withMspUser.Clone(),
		withCustomerTenant: _q.withCustomerTenant.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithMspUser tells the query-builder to eager-load the nodes that are connected to
// the "msp_user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MSPAllocationQuery) WithMspUser(opts ...func(*UserQuery)) *MSPAllocationQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMspUser = query
	return _q
}

// WithCustomerTenant tells the query-builder to eager-load the nodes that are connected to
// the "customer_tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *MSPAllocationQuery) WithCustomerTenant(opts ...func(*TenantQuery)) *MSPAllocationQuery {
	query := (&TenantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCustomerTenant = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MspUserID int `json:"msp_user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MSPAllocation.Query().
//		GroupBy(mspallocation.FieldMspUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *MSPAllocationQuery) GroupBy(field string, fields ...string) *MSPAllocationGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MSPAllocationGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = mspallocation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MspUserID int `json:"msp_user_id,omitempty"`
//	}
//
//	client.MSPAllocation.Query().
//		Select(mspallocation.FieldMspUserID).
//		Scan(ctx, &v)
func (_q *MSPAllocationQuery) Select(fields ...string) *MSPAllocationSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &MSPAllocationSelect{MSPAllocationQuery: _q}
	sbuild.label = mspallocation.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MSPAllocationSelect configured with the given aggregations.
func (_q *MSPAllocationQuery) Aggregate(fns ...AggregateFunc) *MSPAllocationSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *MSPAllocationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !mspallocation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *MSPAllocationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MSPAllocation, error) {
	var (
		nodes       = []*MSPAllocation{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withMspUser != nil,
			_q.withCustomerTenant != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MSPAllocation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MSPAllocation{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withMspUser; query != nil {
		if err := _q.loadMspUser(ctx, query, nodes, nil,
			func(n *MSPAllocation, e *User) { n.Edges.MspUser = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCustomerTenant; query != nil {
		if err := _q.loadCustomerTenant(ctx, query, nodes,
			func(n *MSPAllocation) { n.Edges.CustomerTenant = []*Tenant{} },
			func(n *MSPAllocation, e *Tenant) { n.Edges.CustomerTenant = append(n.Edges.CustomerTenant, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *MSPAllocationQuery) loadMspUser(ctx context.Context, query *UserQuery, nodes []*MSPAllocation, init func(*MSPAllocation), assign func(*MSPAllocation, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*MSPAllocation)
	for i := range nodes {
		fk := nodes[i].MspUserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "msp_user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *MSPAllocationQuery) loadCustomerTenant(ctx context.Context, query *TenantQuery, nodes []*MSPAllocation, init func(*MSPAllocation), assign func(*MSPAllocation, *Tenant)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*MSPAllocation)
	nids := make(map[int]map[*MSPAllocation]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(mspallocation.CustomerTenantTable)
		s.Join(joinT).On(s.C(tenant.FieldID), joinT.C(mspallocation.CustomerTenantPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(mspallocation.CustomerTenantPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(mspallocation.CustomerTenantPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*MSPAllocation]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Tenant](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "customer_tenant" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (_q *MSPAllocationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *MSPAllocationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mspallocation.Table, mspallocation.Columns, sqlgraph.NewFieldSpec(mspallocation.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mspallocation.FieldID)
		for i := range fields {
			if fields[i] != mspallocation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withMspUser != nil {
			_spec.Node.AddColumnOnce(mspallocation.FieldMspUserID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *MSPAllocationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(mspallocation.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = mspallocation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MSPAllocationGroupBy is the group-by builder for MSPAllocation entities.
type MSPAllocationGroupBy struct {
	selector
	build *MSPAllocationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *MSPAllocationGroupBy) Aggregate(fns ...AggregateFunc) *MSPAllocationGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *MSPAllocationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MSPAllocationQuery, *MSPAllocationGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *MSPAllocationGroupBy) sqlScan(ctx context.Context, root *MSPAllocationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MSPAllocationSelect is the builder for selecting fields of MSPAllocation entities.
type MSPAllocationSelect struct {
	*MSPAllocationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *MSPAllocationSelect) Aggregate(fns ...AggregateFunc) *MSPAllocationSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *MSPAllocationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MSPAllocationQuery, *MSPAllocationSelect](ctx, _s.MSPAllocationQuery, _s, _s.inters, v)
}

func (_s *MSPAllocationSelect) sqlScan(ctx context.Context, root *MSPAllocationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
