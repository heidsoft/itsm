// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"itsm-backend/ent/knowledgearticle"
	"itsm-backend/ent/knowledgearticleparticipant"
	"itsm-backend/ent/knowledgearticlesession"
	"itsm-backend/ent/predicate"
	"itsm-backend/ent/user"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// KnowledgeArticleSessionQuery is the builder for querying KnowledgeArticleSession entities.
type KnowledgeArticleSessionQuery struct {
	config
	ctx              *QueryContext
	order            []knowledgearticlesession.OrderOption
	inters           []Interceptor
	predicates       []predicate.KnowledgeArticleSession
	withArticle      *KnowledgeArticleQuery
	withUser         *UserQuery
	withParticipants *KnowledgeArticleParticipantQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the KnowledgeArticleSessionQuery builder.
func (_q *KnowledgeArticleSessionQuery) Where(ps ...predicate.KnowledgeArticleSession) *KnowledgeArticleSessionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *KnowledgeArticleSessionQuery) Limit(limit int) *KnowledgeArticleSessionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *KnowledgeArticleSessionQuery) Offset(offset int) *KnowledgeArticleSessionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *KnowledgeArticleSessionQuery) Unique(unique bool) *KnowledgeArticleSessionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *KnowledgeArticleSessionQuery) Order(o ...knowledgearticlesession.OrderOption) *KnowledgeArticleSessionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryArticle chains the current query on the "article" edge.
func (_q *KnowledgeArticleSessionQuery) QueryArticle() *KnowledgeArticleQuery {
	query := (&KnowledgeArticleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(knowledgearticlesession.Table, knowledgearticlesession.FieldID, selector),
			sqlgraph.To(knowledgearticle.Table, knowledgearticle.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, knowledgearticlesession.ArticleTable, knowledgearticlesession.ArticleColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (_q *KnowledgeArticleSessionQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(knowledgearticlesession.Table, knowledgearticlesession.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, knowledgearticlesession.UserTable, knowledgearticlesession.UserPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParticipants chains the current query on the "participants" edge.
func (_q *KnowledgeArticleSessionQuery) QueryParticipants() *KnowledgeArticleParticipantQuery {
	query := (&KnowledgeArticleParticipantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(knowledgearticlesession.Table, knowledgearticlesession.FieldID, selector),
			sqlgraph.To(knowledgearticleparticipant.Table, knowledgearticleparticipant.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, knowledgearticlesession.ParticipantsTable, knowledgearticlesession.ParticipantsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first KnowledgeArticleSession entity from the query.
// Returns a *NotFoundError when no KnowledgeArticleSession was found.
func (_q *KnowledgeArticleSessionQuery) First(ctx context.Context) (*KnowledgeArticleSession, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{knowledgearticlesession.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *KnowledgeArticleSessionQuery) FirstX(ctx context.Context) *KnowledgeArticleSession {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first KnowledgeArticleSession ID from the query.
// Returns a *NotFoundError when no KnowledgeArticleSession ID was found.
func (_q *KnowledgeArticleSessionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{knowledgearticlesession.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *KnowledgeArticleSessionQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single KnowledgeArticleSession entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one KnowledgeArticleSession entity is found.
// Returns a *NotFoundError when no KnowledgeArticleSession entities are found.
func (_q *KnowledgeArticleSessionQuery) Only(ctx context.Context) (*KnowledgeArticleSession, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{knowledgearticlesession.Label}
	default:
		return nil, &NotSingularError{knowledgearticlesession.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *KnowledgeArticleSessionQuery) OnlyX(ctx context.Context) *KnowledgeArticleSession {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only KnowledgeArticleSession ID in the query.
// Returns a *NotSingularError when more than one KnowledgeArticleSession ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *KnowledgeArticleSessionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{knowledgearticlesession.Label}
	default:
		err = &NotSingularError{knowledgearticlesession.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *KnowledgeArticleSessionQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of KnowledgeArticleSessions.
func (_q *KnowledgeArticleSessionQuery) All(ctx context.Context) ([]*KnowledgeArticleSession, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*KnowledgeArticleSession, *KnowledgeArticleSessionQuery]()
	return withInterceptors[[]*KnowledgeArticleSession](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *KnowledgeArticleSessionQuery) AllX(ctx context.Context) []*KnowledgeArticleSession {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of KnowledgeArticleSession IDs.
func (_q *KnowledgeArticleSessionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(knowledgearticlesession.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *KnowledgeArticleSessionQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *KnowledgeArticleSessionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*KnowledgeArticleSessionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *KnowledgeArticleSessionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *KnowledgeArticleSessionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *KnowledgeArticleSessionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the KnowledgeArticleSessionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *KnowledgeArticleSessionQuery) Clone() *KnowledgeArticleSessionQuery {
	if _q == nil {
		return nil
	}
	return &KnowledgeArticleSessionQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]knowledgearticlesession.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.KnowledgeArticleSession{}, _q.predicates...),
		withArticle:      _q.withArticle.Clone(),
		withUser:         _q.withUser.Clone(),
		withParticipants: _q.withParticipants.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithArticle tells the query-builder to eager-load the nodes that are connected to
// the "article" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *KnowledgeArticleSessionQuery) WithArticle(opts ...func(*KnowledgeArticleQuery)) *KnowledgeArticleSessionQuery {
	query := (&KnowledgeArticleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withArticle = query
	return _q
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *KnowledgeArticleSessionQuery) WithUser(opts ...func(*UserQuery)) *KnowledgeArticleSessionQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithParticipants tells the query-builder to eager-load the nodes that are connected to
// the "participants" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *KnowledgeArticleSessionQuery) WithParticipants(opts ...func(*KnowledgeArticleParticipantQuery)) *KnowledgeArticleSessionQuery {
	query := (&KnowledgeArticleParticipantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParticipants = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ArticleID int `json:"article_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.KnowledgeArticleSession.Query().
//		GroupBy(knowledgearticlesession.FieldArticleID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *KnowledgeArticleSessionQuery) GroupBy(field string, fields ...string) *KnowledgeArticleSessionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &KnowledgeArticleSessionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = knowledgearticlesession.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ArticleID int `json:"article_id,omitempty"`
//	}
//
//	client.KnowledgeArticleSession.Query().
//		Select(knowledgearticlesession.FieldArticleID).
//		Scan(ctx, &v)
func (_q *KnowledgeArticleSessionQuery) Select(fields ...string) *KnowledgeArticleSessionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &KnowledgeArticleSessionSelect{KnowledgeArticleSessionQuery: _q}
	sbuild.label = knowledgearticlesession.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a KnowledgeArticleSessionSelect configured with the given aggregations.
func (_q *KnowledgeArticleSessionQuery) Aggregate(fns ...AggregateFunc) *KnowledgeArticleSessionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *KnowledgeArticleSessionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !knowledgearticlesession.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *KnowledgeArticleSessionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*KnowledgeArticleSession, error) {
	var (
		nodes       = []*KnowledgeArticleSession{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withArticle != nil,
			_q.withUser != nil,
			_q.withParticipants != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*KnowledgeArticleSession).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &KnowledgeArticleSession{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withArticle; query != nil {
		if err := _q.loadArticle(ctx, query, nodes,
			func(n *KnowledgeArticleSession) { n.Edges.Article = []*KnowledgeArticle{} },
			func(n *KnowledgeArticleSession, e *KnowledgeArticle) { n.Edges.Article = append(n.Edges.Article, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes,
			func(n *KnowledgeArticleSession) { n.Edges.User = []*User{} },
			func(n *KnowledgeArticleSession, e *User) { n.Edges.User = append(n.Edges.User, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withParticipants; query != nil {
		if err := _q.loadParticipants(ctx, query, nodes,
			func(n *KnowledgeArticleSession) { n.Edges.Participants = []*KnowledgeArticleParticipant{} },
			func(n *KnowledgeArticleSession, e *KnowledgeArticleParticipant) {
				n.Edges.Participants = append(n.Edges.Participants, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *KnowledgeArticleSessionQuery) loadArticle(ctx context.Context, query *KnowledgeArticleQuery, nodes []*KnowledgeArticleSession, init func(*KnowledgeArticleSession), assign func(*KnowledgeArticleSession, *KnowledgeArticle)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*KnowledgeArticleSession)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.KnowledgeArticle(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(knowledgearticlesession.ArticleColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.knowledge_article_sessions
		if fk == nil {
			return fmt.Errorf(`foreign-key "knowledge_article_sessions" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "knowledge_article_sessions" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *KnowledgeArticleSessionQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*KnowledgeArticleSession, init func(*KnowledgeArticleSession), assign func(*KnowledgeArticleSession, *User)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*KnowledgeArticleSession)
	nids := make(map[int]map[*KnowledgeArticleSession]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(knowledgearticlesession.UserTable)
		s.Join(joinT).On(s.C(user.FieldID), joinT.C(knowledgearticlesession.UserPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(knowledgearticlesession.UserPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(knowledgearticlesession.UserPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*KnowledgeArticleSession]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*User](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "user" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *KnowledgeArticleSessionQuery) loadParticipants(ctx context.Context, query *KnowledgeArticleParticipantQuery, nodes []*KnowledgeArticleSession, init func(*KnowledgeArticleSession), assign func(*KnowledgeArticleSession, *KnowledgeArticleParticipant)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*KnowledgeArticleSession)
	nids := make(map[int]map[*KnowledgeArticleSession]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(knowledgearticlesession.ParticipantsTable)
		s.Join(joinT).On(s.C(knowledgearticleparticipant.FieldID), joinT.C(knowledgearticlesession.ParticipantsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(knowledgearticlesession.ParticipantsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(knowledgearticlesession.ParticipantsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*KnowledgeArticleSession]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*KnowledgeArticleParticipant](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "participants" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (_q *KnowledgeArticleSessionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *KnowledgeArticleSessionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(knowledgearticlesession.Table, knowledgearticlesession.Columns, sqlgraph.NewFieldSpec(knowledgearticlesession.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, knowledgearticlesession.FieldID)
		for i := range fields {
			if fields[i] != knowledgearticlesession.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *KnowledgeArticleSessionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(knowledgearticlesession.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = knowledgearticlesession.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// KnowledgeArticleSessionGroupBy is the group-by builder for KnowledgeArticleSession entities.
type KnowledgeArticleSessionGroupBy struct {
	selector
	build *KnowledgeArticleSessionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *KnowledgeArticleSessionGroupBy) Aggregate(fns ...AggregateFunc) *KnowledgeArticleSessionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *KnowledgeArticleSessionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KnowledgeArticleSessionQuery, *KnowledgeArticleSessionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *KnowledgeArticleSessionGroupBy) sqlScan(ctx context.Context, root *KnowledgeArticleSessionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// KnowledgeArticleSessionSelect is the builder for selecting fields of KnowledgeArticleSession entities.
type KnowledgeArticleSessionSelect struct {
	*KnowledgeArticleSessionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *KnowledgeArticleSessionSelect) Aggregate(fns ...AggregateFunc) *KnowledgeArticleSessionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *KnowledgeArticleSessionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*KnowledgeArticleSessionQuery, *KnowledgeArticleSessionSelect](ctx, _s.KnowledgeArticleSessionQuery, _s, _s.inters, v)
}

func (_s *KnowledgeArticleSessionSelect) sqlScan(ctx context.Context, root *KnowledgeArticleSessionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
