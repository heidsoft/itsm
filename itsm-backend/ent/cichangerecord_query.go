// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"itsm-backend/ent/cichangerecord"
	"itsm-backend/ent/configurationitem"
	"itsm-backend/ent/predicate"
	"itsm-backend/ent/tenant"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CIChangeRecordQuery is the builder for querying CIChangeRecord entities.
type CIChangeRecordQuery struct {
	config
	ctx                   *QueryContext
	order                 []cichangerecord.OrderOption
	inters                []Interceptor
	predicates            []predicate.CIChangeRecord
	withTenant            *TenantQuery
	withConfigurationItem *ConfigurationItemQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CIChangeRecordQuery builder.
func (ccrq *CIChangeRecordQuery) Where(ps ...predicate.CIChangeRecord) *CIChangeRecordQuery {
	ccrq.predicates = append(ccrq.predicates, ps...)
	return ccrq
}

// Limit the number of records to be returned by this query.
func (ccrq *CIChangeRecordQuery) Limit(limit int) *CIChangeRecordQuery {
	ccrq.ctx.Limit = &limit
	return ccrq
}

// Offset to start from.
func (ccrq *CIChangeRecordQuery) Offset(offset int) *CIChangeRecordQuery {
	ccrq.ctx.Offset = &offset
	return ccrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ccrq *CIChangeRecordQuery) Unique(unique bool) *CIChangeRecordQuery {
	ccrq.ctx.Unique = &unique
	return ccrq
}

// Order specifies how the records should be ordered.
func (ccrq *CIChangeRecordQuery) Order(o ...cichangerecord.OrderOption) *CIChangeRecordQuery {
	ccrq.order = append(ccrq.order, o...)
	return ccrq
}

// QueryTenant chains the current query on the "tenant" edge.
func (ccrq *CIChangeRecordQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: ccrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ccrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ccrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cichangerecord.Table, cichangerecord.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, cichangerecord.TenantTable, cichangerecord.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(ccrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryConfigurationItem chains the current query on the "configuration_item" edge.
func (ccrq *CIChangeRecordQuery) QueryConfigurationItem() *ConfigurationItemQuery {
	query := (&ConfigurationItemClient{config: ccrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ccrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ccrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cichangerecord.Table, cichangerecord.FieldID, selector),
			sqlgraph.To(configurationitem.Table, configurationitem.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, cichangerecord.ConfigurationItemTable, cichangerecord.ConfigurationItemColumn),
		)
		fromU = sqlgraph.SetNeighbors(ccrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CIChangeRecord entity from the query.
// Returns a *NotFoundError when no CIChangeRecord was found.
func (ccrq *CIChangeRecordQuery) First(ctx context.Context) (*CIChangeRecord, error) {
	nodes, err := ccrq.Limit(1).All(setContextOp(ctx, ccrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cichangerecord.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ccrq *CIChangeRecordQuery) FirstX(ctx context.Context) *CIChangeRecord {
	node, err := ccrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CIChangeRecord ID from the query.
// Returns a *NotFoundError when no CIChangeRecord ID was found.
func (ccrq *CIChangeRecordQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ccrq.Limit(1).IDs(setContextOp(ctx, ccrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cichangerecord.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ccrq *CIChangeRecordQuery) FirstIDX(ctx context.Context) int {
	id, err := ccrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CIChangeRecord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CIChangeRecord entity is found.
// Returns a *NotFoundError when no CIChangeRecord entities are found.
func (ccrq *CIChangeRecordQuery) Only(ctx context.Context) (*CIChangeRecord, error) {
	nodes, err := ccrq.Limit(2).All(setContextOp(ctx, ccrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cichangerecord.Label}
	default:
		return nil, &NotSingularError{cichangerecord.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ccrq *CIChangeRecordQuery) OnlyX(ctx context.Context) *CIChangeRecord {
	node, err := ccrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CIChangeRecord ID in the query.
// Returns a *NotSingularError when more than one CIChangeRecord ID is found.
// Returns a *NotFoundError when no entities are found.
func (ccrq *CIChangeRecordQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ccrq.Limit(2).IDs(setContextOp(ctx, ccrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cichangerecord.Label}
	default:
		err = &NotSingularError{cichangerecord.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ccrq *CIChangeRecordQuery) OnlyIDX(ctx context.Context) int {
	id, err := ccrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CIChangeRecords.
func (ccrq *CIChangeRecordQuery) All(ctx context.Context) ([]*CIChangeRecord, error) {
	ctx = setContextOp(ctx, ccrq.ctx, ent.OpQueryAll)
	if err := ccrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CIChangeRecord, *CIChangeRecordQuery]()
	return withInterceptors[[]*CIChangeRecord](ctx, ccrq, qr, ccrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ccrq *CIChangeRecordQuery) AllX(ctx context.Context) []*CIChangeRecord {
	nodes, err := ccrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CIChangeRecord IDs.
func (ccrq *CIChangeRecordQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ccrq.ctx.Unique == nil && ccrq.path != nil {
		ccrq.Unique(true)
	}
	ctx = setContextOp(ctx, ccrq.ctx, ent.OpQueryIDs)
	if err = ccrq.Select(cichangerecord.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ccrq *CIChangeRecordQuery) IDsX(ctx context.Context) []int {
	ids, err := ccrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ccrq *CIChangeRecordQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ccrq.ctx, ent.OpQueryCount)
	if err := ccrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ccrq, querierCount[*CIChangeRecordQuery](), ccrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ccrq *CIChangeRecordQuery) CountX(ctx context.Context) int {
	count, err := ccrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ccrq *CIChangeRecordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ccrq.ctx, ent.OpQueryExist)
	switch _, err := ccrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ccrq *CIChangeRecordQuery) ExistX(ctx context.Context) bool {
	exist, err := ccrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CIChangeRecordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ccrq *CIChangeRecordQuery) Clone() *CIChangeRecordQuery {
	if ccrq == nil {
		return nil
	}
	return &CIChangeRecordQuery{
		config:                ccrq.config,
		ctx:                   ccrq.ctx.Clone(),
		order:                 append([]cichangerecord.OrderOption{}, ccrq.order...),
		inters:                append([]Interceptor{}, ccrq.inters...),
		predicates:            append([]predicate.CIChangeRecord{}, ccrq.predicates...),
		withTenant:            ccrq.withTenant.Clone(),
		withConfigurationItem: ccrq.withConfigurationItem.Clone(),
		// clone intermediate query.
		sql:  ccrq.sql.Clone(),
		path: ccrq.path,
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (ccrq *CIChangeRecordQuery) WithTenant(opts ...func(*TenantQuery)) *CIChangeRecordQuery {
	query := (&TenantClient{config: ccrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ccrq.withTenant = query
	return ccrq
}

// WithConfigurationItem tells the query-builder to eager-load the nodes that are connected to
// the "configuration_item" edge. The optional arguments are used to configure the query builder of the edge.
func (ccrq *CIChangeRecordQuery) WithConfigurationItem(opts ...func(*ConfigurationItemQuery)) *CIChangeRecordQuery {
	query := (&ConfigurationItemClient{config: ccrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ccrq.withConfigurationItem = query
	return ccrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CiID int `json:"ci_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CIChangeRecord.Query().
//		GroupBy(cichangerecord.FieldCiID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ccrq *CIChangeRecordQuery) GroupBy(field string, fields ...string) *CIChangeRecordGroupBy {
	ccrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CIChangeRecordGroupBy{build: ccrq}
	grbuild.flds = &ccrq.ctx.Fields
	grbuild.label = cichangerecord.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CiID int `json:"ci_id,omitempty"`
//	}
//
//	client.CIChangeRecord.Query().
//		Select(cichangerecord.FieldCiID).
//		Scan(ctx, &v)
func (ccrq *CIChangeRecordQuery) Select(fields ...string) *CIChangeRecordSelect {
	ccrq.ctx.Fields = append(ccrq.ctx.Fields, fields...)
	sbuild := &CIChangeRecordSelect{CIChangeRecordQuery: ccrq}
	sbuild.label = cichangerecord.Label
	sbuild.flds, sbuild.scan = &ccrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CIChangeRecordSelect configured with the given aggregations.
func (ccrq *CIChangeRecordQuery) Aggregate(fns ...AggregateFunc) *CIChangeRecordSelect {
	return ccrq.Select().Aggregate(fns...)
}

func (ccrq *CIChangeRecordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ccrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ccrq); err != nil {
				return err
			}
		}
	}
	for _, f := range ccrq.ctx.Fields {
		if !cichangerecord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ccrq.path != nil {
		prev, err := ccrq.path(ctx)
		if err != nil {
			return err
		}
		ccrq.sql = prev
	}
	return nil
}

func (ccrq *CIChangeRecordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CIChangeRecord, error) {
	var (
		nodes       = []*CIChangeRecord{}
		_spec       = ccrq.querySpec()
		loadedTypes = [2]bool{
			ccrq.withTenant != nil,
			ccrq.withConfigurationItem != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CIChangeRecord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CIChangeRecord{config: ccrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ccrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ccrq.withTenant; query != nil {
		if err := ccrq.loadTenant(ctx, query, nodes, nil,
			func(n *CIChangeRecord, e *Tenant) { n.Edges.Tenant = e }); err != nil {
			return nil, err
		}
	}
	if query := ccrq.withConfigurationItem; query != nil {
		if err := ccrq.loadConfigurationItem(ctx, query, nodes, nil,
			func(n *CIChangeRecord, e *ConfigurationItem) { n.Edges.ConfigurationItem = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ccrq *CIChangeRecordQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*CIChangeRecord, init func(*CIChangeRecord), assign func(*CIChangeRecord, *Tenant)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CIChangeRecord)
	for i := range nodes {
		fk := nodes[i].TenantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(tenant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "tenant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ccrq *CIChangeRecordQuery) loadConfigurationItem(ctx context.Context, query *ConfigurationItemQuery, nodes []*CIChangeRecord, init func(*CIChangeRecord), assign func(*CIChangeRecord, *ConfigurationItem)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CIChangeRecord)
	for i := range nodes {
		fk := nodes[i].CiID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(configurationitem.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "ci_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ccrq *CIChangeRecordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ccrq.querySpec()
	_spec.Node.Columns = ccrq.ctx.Fields
	if len(ccrq.ctx.Fields) > 0 {
		_spec.Unique = ccrq.ctx.Unique != nil && *ccrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ccrq.driver, _spec)
}

func (ccrq *CIChangeRecordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(cichangerecord.Table, cichangerecord.Columns, sqlgraph.NewFieldSpec(cichangerecord.FieldID, field.TypeInt))
	_spec.From = ccrq.sql
	if unique := ccrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ccrq.path != nil {
		_spec.Unique = true
	}
	if fields := ccrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cichangerecord.FieldID)
		for i := range fields {
			if fields[i] != cichangerecord.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ccrq.withTenant != nil {
			_spec.Node.AddColumnOnce(cichangerecord.FieldTenantID)
		}
		if ccrq.withConfigurationItem != nil {
			_spec.Node.AddColumnOnce(cichangerecord.FieldCiID)
		}
	}
	if ps := ccrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ccrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ccrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ccrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ccrq *CIChangeRecordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ccrq.driver.Dialect())
	t1 := builder.Table(cichangerecord.Table)
	columns := ccrq.ctx.Fields
	if len(columns) == 0 {
		columns = cichangerecord.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ccrq.sql != nil {
		selector = ccrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ccrq.ctx.Unique != nil && *ccrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ccrq.predicates {
		p(selector)
	}
	for _, p := range ccrq.order {
		p(selector)
	}
	if offset := ccrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ccrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CIChangeRecordGroupBy is the group-by builder for CIChangeRecord entities.
type CIChangeRecordGroupBy struct {
	selector
	build *CIChangeRecordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ccrgb *CIChangeRecordGroupBy) Aggregate(fns ...AggregateFunc) *CIChangeRecordGroupBy {
	ccrgb.fns = append(ccrgb.fns, fns...)
	return ccrgb
}

// Scan applies the selector query and scans the result into the given value.
func (ccrgb *CIChangeRecordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccrgb.build.ctx, ent.OpQueryGroupBy)
	if err := ccrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CIChangeRecordQuery, *CIChangeRecordGroupBy](ctx, ccrgb.build, ccrgb, ccrgb.build.inters, v)
}

func (ccrgb *CIChangeRecordGroupBy) sqlScan(ctx context.Context, root *CIChangeRecordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ccrgb.fns))
	for _, fn := range ccrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ccrgb.flds)+len(ccrgb.fns))
		for _, f := range *ccrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ccrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CIChangeRecordSelect is the builder for selecting fields of CIChangeRecord entities.
type CIChangeRecordSelect struct {
	*CIChangeRecordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ccrs *CIChangeRecordSelect) Aggregate(fns ...AggregateFunc) *CIChangeRecordSelect {
	ccrs.fns = append(ccrs.fns, fns...)
	return ccrs
}

// Scan applies the selector query and scans the result into the given value.
func (ccrs *CIChangeRecordSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccrs.ctx, ent.OpQuerySelect)
	if err := ccrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CIChangeRecordQuery, *CIChangeRecordSelect](ctx, ccrs.CIChangeRecordQuery, ccrs, ccrs.inters, v)
}

func (ccrs *CIChangeRecordSelect) sqlScan(ctx context.Context, root *CIChangeRecordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ccrs.fns))
	for _, fn := range ccrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ccrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
