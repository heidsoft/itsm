# ITSM 平台优化目录结构

**日期**: 2025-11-21  
**场景**: 大型 ITSM 平台，多个独立功能模块  
**原则**: 模块平铺、职责清晰、易于扩展

---

## 📁 推荐的 ITSM 平台目录结构

```
src/
├── app/                              # Next.js App Router
│   │
│   ├── (auth)/                       # 路由组：认证相关（公开访问）
│   │   ├── login/
│   │   │   └── page.tsx             # /login
│   │   ├── register/
│   │   │   └── page.tsx             # /register
│   │   ├── forgot-password/
│   │   │   └── page.tsx             # /forgot-password
│   │   └── layout.tsx               # 认证页面的简洁布局
│   │
│   ├── (main)/                       # 路由组：主应用（需要认证）
│   │   │
│   │   ├── dashboard/                # 仪表盘模块
│   │   │   ├── page.tsx             # /dashboard - 主仪表盘
│   │   │   ├── analytics/           
│   │   │   │   └── page.tsx         # /dashboard/analytics
│   │   │   └── reports/
│   │   │       └── page.tsx         # /dashboard/reports
│   │   │
│   │   ├── tickets/                  # 工单管理模块 ⭐
│   │   │   ├── page.tsx             # /tickets - 工单列表
│   │   │   ├── [id]/               
│   │   │   │   ├── page.tsx         # /tickets/123 - 工单详情
│   │   │   │   └── edit/
│   │   │   │       └── page.tsx     # /tickets/123/edit
│   │   │   ├── new/
│   │   │   │   └── page.tsx         # /tickets/new - 创建工单
│   │   │   ├── templates/
│   │   │   │   └── page.tsx         # /tickets/templates
│   │   │   ├── bulk/
│   │   │   │   └── page.tsx         # /tickets/bulk - 批量操作
│   │   │   └── layout.tsx           # 工单模块布局
│   │   │
│   │   ├── incidents/                # 事件管理模块 ⭐
│   │   │   ├── page.tsx             # /incidents - 事件列表
│   │   │   ├── [id]/
│   │   │   │   └── page.tsx         # /incidents/123
│   │   │   ├── new/
│   │   │   │   └── page.tsx         # /incidents/new
│   │   │   ├── major/               # 重大事件
│   │   │   │   └── page.tsx         # /incidents/major
│   │   │   └── layout.tsx
│   │   │
│   │   ├── problems/                 # 问题管理模块 ⭐
│   │   │   ├── page.tsx             # /problems - 问题列表
│   │   │   ├── [id]/
│   │   │   │   └── page.tsx         # /problems/123
│   │   │   ├── new/
│   │   │   │   └── page.tsx         # /problems/new
│   │   │   ├── investigations/      # 问题调查
│   │   │   │   └── page.tsx
│   │   │   └── layout.tsx
│   │   │
│   │   ├── changes/                  # 变更管理模块 ⭐
│   │   │   ├── page.tsx             # /changes - 变更列表
│   │   │   ├── [id]/
│   │   │   │   └── page.tsx         # /changes/123
│   │   │   ├── new/
│   │   │   │   └── page.tsx         # /changes/new
│   │   │   ├── calendar/            # 变更日历
│   │   │   │   └── page.tsx         # /changes/calendar
│   │   │   ├── approval/            # 变更审批
│   │   │   │   └── page.tsx
│   │   │   └── layout.tsx
│   │   │
│   │   ├── cmdb/                     # 配置管理数据库 ⭐
│   │   │   ├── page.tsx             # /cmdb - CMDB 主页
│   │   │   ├── items/
│   │   │   │   ├── page.tsx         # /cmdb/items - CI列表
│   │   │   │   ├── [id]/
│   │   │   │   │   └── page.tsx     # /cmdb/items/123
│   │   │   │   └── new/
│   │   │   │       └── page.tsx
│   │   │   ├── relationships/        # CI关系图
│   │   │   │   └── page.tsx
│   │   │   ├── topology/            # 拓扑视图
│   │   │   │   └── page.tsx
│   │   │   └── layout.tsx
│   │   │
│   │   ├── knowledge-base/           # 知识库模块 ⭐
│   │   │   ├── page.tsx             # /knowledge-base
│   │   │   ├── articles/
│   │   │   │   ├── page.tsx         # /knowledge-base/articles
│   │   │   │   ├── [id]/
│   │   │   │   │   └── page.tsx
│   │   │   │   └── new/
│   │   │   │       └── page.tsx
│   │   │   ├── categories/
│   │   │   │   └── page.tsx
│   │   │   └── search/
│   │   │       └── page.tsx
│   │   │
│   │   ├── service-catalog/          # 服务目录模块 ⭐
│   │   │   ├── page.tsx             # /service-catalog
│   │   │   ├── services/
│   │   │   │   ├── page.tsx         # 服务列表
│   │   │   │   └── [id]/
│   │   │   │       └── page.tsx     # 服务详情
│   │   │   ├── requests/            # 服务请求
│   │   │   │   └── page.tsx
│   │   │   └── layout.tsx
│   │   │
│   │   ├── sla/                      # SLA管理模块 ⭐
│   │   │   ├── page.tsx             # /sla - SLA概览
│   │   │   ├── definitions/
│   │   │   │   └── page.tsx         # SLA定义
│   │   │   ├── monitoring/
│   │   │   │   └── page.tsx         # SLA监控
│   │   │   └── reports/
│   │   │       └── page.tsx         # SLA报告
│   │   │
│   │   ├── reports/                  # 报告模块 ⭐
│   │   │   ├── page.tsx             # /reports
│   │   │   ├── tickets/
│   │   │   │   └── page.tsx
│   │   │   ├── incidents/
│   │   │   │   └── page.tsx
│   │   │   ├── performance/
│   │   │   │   └── page.tsx
│   │   │   └── custom/
│   │   │       └── page.tsx
│   │   │
│   │   ├── admin/                    # 管理后台模块 ⭐
│   │   │   ├── page.tsx             # /admin
│   │   │   ├── users/
│   │   │   │   └── page.tsx         # 用户管理
│   │   │   ├── roles/
│   │   │   │   └── page.tsx         # 角色管理
│   │   │   ├── tenants/
│   │   │   │   └── page.tsx         # 租户管理
│   │   │   ├── settings/
│   │   │   │   └── page.tsx         # 系统设置
│   │   │   └── layout.tsx
│   │   │
│   │   ├── profile/                  # 个人中心
│   │   │   ├── page.tsx             # /profile
│   │   │   ├── settings/
│   │   │   │   └── page.tsx
│   │   │   └── notifications/
│   │   │       └── page.tsx
│   │   │
│   │   └── layout.tsx               # (main) 的主布局，包含：
│   │                                 # - Header
│   │                                 # - Sidebar（模块导航）
│   │                                 # - Footer
│   │
│   ├── api/                          # API Routes (如果需要)
│   │   ├── webhooks/
│   │   │   └── route.ts
│   │   └── health/
│   │       └── route.ts
│   │
│   ├── layout.tsx                    # 根布局
│   ├── page.tsx                      # 首页 (/) - 重定向到 /dashboard
│   ├── globals.css
│   ├── error.tsx                     # 全局错误页面
│   ├── loading.tsx                   # 全局加载页面
│   └── not-found.tsx                 # 404 页面
│
├── components/                       # 组件库
│   ├── ui/                          # 基础UI组件
│   │   ├── button/
│   │   ├── card/
│   │   ├── table/
│   │   ├── form/
│   │   └── index.ts
│   │
│   ├── features/                    # 功能组件（按模块分类）
│   │   ├── ticket/                  # 工单相关组件
│   │   │   ├── TicketList.tsx
│   │   │   ├── TicketCard.tsx
│   │   │   ├── TicketForm.tsx
│   │   │   ├── TicketFilters.tsx
│   │   │   ├── TicketStats.tsx
│   │   │   └── index.ts
│   │   ├── incident/                # 事件相关组件
│   │   │   ├── IncidentList.tsx
│   │   │   ├── IncidentTimeline.tsx
│   │   │   └── index.ts
│   │   ├── problem/                 # 问题相关组件
│   │   ├── change/                  # 变更相关组件
│   │   ├── cmdb/                    # CMDB相关组件
│   │   │   ├── CICard.tsx
│   │   │   ├── TopologyView.tsx
│   │   │   └── index.ts
│   │   ├── dashboard/               # 仪表盘组件
│   │   │   ├── KPICard.tsx
│   │   │   ├── ChartWidget.tsx
│   │   │   └── index.ts
│   │   └── index.ts
│   │
│   ├── layout/                      # 布局组件
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   ├── Footer.tsx
│   │   ├── Navigation.tsx
│   │   └── index.ts
│   │
│   ├── common/                      # 通用组件
│   │   ├── ErrorBoundary.tsx
│   │   ├── LoadingState.tsx
│   │   ├── EmptyState.tsx
│   │   └── index.ts
│   │
│   └── providers/                   # Context Providers
│       ├── ThemeProvider.tsx
│       ├── QueryProvider.tsx
│       └── index.ts
│
├── lib/                             # 核心库
│   ├── api/                         # API层
│   │   ├── client.ts               # HTTP客户端（唯一）
│   │   ├── endpoints/              # API端点（按模块分类）
│   │   │   ├── tickets.ts
│   │   │   ├── incidents.ts
│   │   │   ├── problems.ts
│   │   │   ├── changes.ts
│   │   │   ├── cmdb.ts
│   │   │   ├── users.ts
│   │   │   └── index.ts
│   │   └── index.ts
│   │
│   ├── hooks/                       # 自定义Hooks
│   │   ├── queries/                # React Query hooks（按模块）
│   │   │   ├── tickets/
│   │   │   │   ├── useTickets.ts
│   │   │   │   ├── useTicketQuery.ts
│   │   │   │   └── index.ts
│   │   │   ├── incidents/
│   │   │   ├── problems/
│   │   │   ├── changes/
│   │   │   └── index.ts
│   │   ├── mutations/              # Mutation hooks
│   │   │   ├── tickets/
│   │   │   ├── incidents/
│   │   │   └── index.ts
│   │   ├── useAuth.ts
│   │   ├── usePermissions.ts
│   │   └── index.ts
│   │
│   ├── stores/                      # 状态管理（Zustand）
│   │   ├── auth.ts
│   │   ├── ui.ts
│   │   ├── tenant.ts
│   │   └── index.ts
│   │
│   ├── utils/
│   ├── constants/
│   ├── config/
│   └── services/
│
├── types/                           # TypeScript类型
│   ├── models/                      # 数据模型（按模块）
│   │   ├── ticket.ts
│   │   ├── incident.ts
│   │   ├── problem.ts
│   │   ├── change.ts
│   │   ├── cmdb.ts
│   │   └── index.ts
│   └── index.ts
│
└── middleware.ts                    # Next.js中间件
```

---

## 🎯 设计原则

### 1. **模块平铺，不嵌套**

```
✅ 推荐（平铺）:
app/
  (main)/
    dashboard/      # 仪表盘是一个模块
    tickets/        # 工单是一个模块
    incidents/      # 事件是一个模块
    problems/       # 问题是一个模块

❌ 不推荐（嵌套）:
app/
  (dashboard)/      # dashboard不应该是父级
    dashboard/
    tickets/
    incidents/
```

**为什么？**

- ITSM 的每个模块都是**独立的子系统**，不是从属关系
- 模块间是**平级关系**，而非父子关系
- 便于**独立开发和维护**

---

### 2. **路由组的正确使用**

```typescript
// (auth) 路由组 - 认证相关页面
app/(auth)/login/page.tsx      → /login
app/(auth)/register/page.tsx   → /register

// (main) 路由组 - 主应用页面（需要认证）
app/(main)/tickets/page.tsx    → /tickets
app/(main)/incidents/page.tsx  → /incidents

// 路由组不影响URL路径！
// (auth) 和 (main) 不会出现在URL中
```

**路由组的作用**：

- ✅ 分组管理路由
- ✅ 应用不同的布局（layout.tsx）
- ✅ 不影响实际的URL路径

---

### 3. **每个模块的标准结构**

以 `tickets/` 模块为例：

```
tickets/
├── page.tsx              # 列表页
├── [id]/                # 动态路由（详情页）
│   ├── page.tsx
│   └── edit/
│       └── page.tsx
├── new/                 # 创建页
│   └── page.tsx
├── templates/           # 子功能
│   └── page.tsx
├── bulk/                # 批量操作
│   └── page.tsx
└── layout.tsx           # 模块布局（可选）
```

**统一的模式**：

- `page.tsx` - 列表页
- `[id]/page.tsx` - 详情页
- `new/page.tsx` - 创建页
- 其他子功能 - 独立目录

---

### 4. **URL 结构清晰**

```
/                           → 首页（重定向到 /dashboard）
/login                      → 登录页
/register                   → 注册页

# 主应用
/dashboard                  → 仪表盘
/dashboard/analytics        → 分析页

# 工单模块
/tickets                    → 工单列表
/tickets/123               → 工单详情
/tickets/123/edit          → 编辑工单
/tickets/new               → 创建工单
/tickets/templates         → 工单模板

# 事件模块
/incidents                 → 事件列表
/incidents/123            → 事件详情
/incidents/major          → 重大事件

# CMDB模块
/cmdb                      → CMDB主页
/cmdb/items               → CI列表
/cmdb/items/123           → CI详情
/cmdb/topology            → 拓扑视图

# 管理后台
/admin                     → 管理主页
/admin/users              → 用户管理
/admin/roles              → 角色管理
```

---

## 📊 对比：原结构 vs 优化结构

| 方面 | 原结构建议 | 优化结构 | 优势 |
|------|-----------|---------|------|
| **模块组织** | 嵌套在 (dashboard)/ 下 | 平铺在 (main)/ 下 | ✅ 模块独立性强 |
| **URL清晰度** | 不确定是否需要前缀 | 清晰的扁平结构 | ✅ 用户友好 |
| **可扩展性** | 受限于父级结构 | 容易添加新模块 | ✅ 易于扩展 |
| **开发效率** | 需要理解嵌套关系 | 直观的模块划分 | ✅ 快速定位 |
| **模块独立性** | 低（从属于dashboard） | 高（完全独立） | ✅ 便于维护 |

---

## 🚀 实施建议

### 阶段1：调整 app/ 目录结构

```bash
# 1. 创建路由组
mkdir -p src/app/\(auth\)
mkdir -p src/app/\(main\)

# 2. 移动认证页面
mv src/app/login src/app/\(auth\)/
mv src/app/register src/app/\(auth\)/  # 如果有

# 3. 移动主应用页面（保持平铺）
mv src/app/dashboard src/app/\(main\)/
mv src/app/tickets src/app/\(main\)/
mv src/app/incidents src/app/\(main\)/
mv src/app/problems src/app/\(main\)/
mv src/app/changes src/app/\(main\)/
mv src/app/cmdb src/app/\(main\)/
# ... 其他模块

# 4. 创建布局文件
# src/app/(auth)/layout.tsx - 简洁的认证布局
# src/app/(main)/layout.tsx - 主应用布局（包含sidebar）
```

### 阶段2：优化组件结构

```typescript
// components/features/ 按模块组织
components/
  features/
    ticket/         # 工单相关的所有组件
      TicketList.tsx
      TicketCard.tsx
      TicketForm.tsx
      TicketFilters.tsx
      TicketStats.tsx
    incident/       # 事件相关的所有组件
    problem/        # 问题相关的所有组件
    change/         # 变更相关的所有组件
    cmdb/          # CMDB相关的所有组件
```

### 阶段3：优化API和Hooks

```typescript
// lib/api/endpoints/ 按模块组织
lib/
  api/
    endpoints/
      tickets.ts      # 工单相关的所有API
      incidents.ts    # 事件相关的所有API
      problems.ts     # 问题相关的所有API
      
// lib/hooks/queries/ 按模块组织
lib/
  hooks/
    queries/
      tickets/        # 工单相关的所有hooks
        useTickets.ts
        useTicketQuery.ts
        useTicketStats.ts
```

---

## 💡 关键点

### 1. **ITSM 平台特点**

- **多模块系统**：不是单一应用，而是多个子系统的集合
- **模块独立**：每个模块都有自己的完整功能（列表、详情、创建、编辑）
- **模块平级**：dashboard、tickets、incidents 等都是平级关系

### 2. **路由组的正确理解**

```typescript
// ❌ 错误理解：路由组影响URL
(dashboard)/tickets → /dashboard/tickets

// ✅ 正确理解：路由组只是逻辑分组
(main)/tickets → /tickets
```

### 3. **布局层级**

```
根布局 (app/layout.tsx)
  └─ 认证布局 (app/(auth)/layout.tsx)  → 简洁布局
  └─ 主应用布局 (app/(main)/layout.tsx) → 包含 Header + Sidebar + Footer
      └─ 模块布局 (app/(main)/tickets/layout.tsx) → 可选，模块特定的布局
```

---

## 📝 示例：Sidebar 导航配置

```typescript
// components/layout/Sidebar.tsx

const navigation = [
  {
    name: '仪表盘',
    icon: DashboardIcon,
    href: '/dashboard',
  },
  {
    name: '工单管理',
    icon: TicketIcon,
    href: '/tickets',
    badge: 5, // 未读数量
  },
  {
    name: '事件管理',
    icon: AlertIcon,
    href: '/incidents',
    badge: 2,
  },
  {
    name: '问题管理',
    icon: BugIcon,
    href: '/problems',
  },
  {
    name: '变更管理',
    icon: GitBranchIcon,
    href: '/changes',
  },
  {
    name: 'CMDB',
    icon: DatabaseIcon,
    href: '/cmdb',
  },
  {
    name: '知识库',
    icon: BookIcon,
    href: '/knowledge-base',
  },
  {
    name: '服务目录',
    icon: GridIcon,
    href: '/service-catalog',
  },
  {
    name: 'SLA管理',
    icon: ClockIcon,
    href: '/sla',
  },
  {
    name: '报告',
    icon: ChartIcon,
    href: '/reports',
  },
  {
    name: '管理后台',
    icon: SettingsIcon,
    href: '/admin',
    permission: 'admin', // 需要管理员权限
  },
];
```

---

## ✅ 总结

### 为什么这个结构更适合ITSM？

1. **✅ 模块独立性** - 每个模块都是独立的子系统
2. **✅ URL清晰** - `/tickets`, `/incidents`, `/problems` 直观易懂
3. **✅ 易于导航** - Sidebar可以直接链接到每个模块
4. **✅ 易于扩展** - 添加新模块只需在 `(main)/` 下新增目录
5. **✅ 团队协作** - 不同团队可以独立维护各自的模块
6. **✅ 符合直觉** - dashboard、tickets、incidents 是平级关系

---

**最后更新**: 2025-11-21  
**适用场景**: 大型ITSM平台、多模块系统  
**建议**: 采用扁平化的模块结构，而非嵌套结构
