# ITSM 设计系统文档

## 📚 概述

ITSM 设计系统是一套完整的设计规范，确保整个系统具有一致的视觉风格和交互体验。本设计系统基于现代设计原则，支持主题切换、响应式设计和无障碍访问。

## 🎨 设计原则

### 1. 一致性 (Consistency)

- 统一的视觉语言和交互模式
- 标准化的组件和布局
- 一致的颜色、字体和间距使用

### 2. 可访问性 (Accessibility)

- 符合 WCAG 2.1 AA 标准
- 支持键盘导航和屏幕阅读器
- 提供高对比度模式

### 3. 响应式 (Responsive)

- 支持各种屏幕尺寸
- 移动优先的设计方法
- 灵活的网格系统

### 4. 可维护性 (Maintainable)

- 模块化的组件架构
- 清晰的代码结构
- 完整的文档和示例

## 🎨 颜色系统

### 主色调 (Primary Colors)

主色调用于品牌标识、主要操作按钮和重要元素。

```css
/* 主色调 */
--color-primary-50: #eff6ff;   /* 最浅 */
--color-primary-100: #dbeafe;
--color-primary-200: #bfdbfe;
--color-primary-300: #93c5fd;
--color-primary-400: #60a5fa;
--color-primary-500: #3b82f6;  /* 主色 */
--color-primary-600: #2563eb;
--color-primary-700: #1d4ed8;
--color-primary-800: #1e40af;
--color-primary-900: #1e3a8a;
--color-primary-950: #172554; /* 最深 */
```

**使用指南：**

- `primary-500` 用于主要按钮、链接和选中状态
- `primary-600` 用于悬停状态
- `primary-100` 用于背景色
- `primary-900` 用于深色文本

### 中性色 (Neutral Colors)

中性色用于文本、边框、背景等基础元素。

```css
/* 中性色 */
--color-neutral-50: #f8fafc;   /* 最浅背景 */
--color-neutral-100: #f1f5f9;
--color-neutral-200: #e2e8f0;  /* 边框 */
--color-neutral-300: #cbd5e1;
--color-neutral-400: #94a3b8;
--color-neutral-500: #64748b;   /* 次要文本 */
--color-neutral-600: #475569;
--color-neutral-700: #334155;
--color-neutral-800: #1e293b;
--color-neutral-900: #0f172a;   /* 主要文本 */
--color-neutral-950: #020617;  /* 最深背景 */
```

**使用指南：**

- `neutral-900` 用于主要文本
- `neutral-600` 用于次要文本
- `neutral-200` 用于边框和分割线
- `neutral-50` 用于页面背景

### 语义色 (Semantic Colors)

语义色用于表示不同的状态和信息类型。

#### 成功色 (Success)

```css
--color-success-500: #22c55e;  /* 成功状态 */
--color-success-100: #dcfce7;  /* 成功背景 */
```

#### 警告色 (Warning)

```css
--color-warning-500: #f59e0b;  /* 警告状态 */
--color-warning-100: #fef3c7;  /* 警告背景 */
```

#### 错误色 (Error)

```css
--color-error-500: #ef4444;    /* 错误状态 */
--color-error-100: #fee2e2;    /* 错误背景 */
```

#### 信息色 (Info)

```css
--color-info-500: #0ea5e9;     /* 信息状态 */
--color-info-100: #e0f2fe;     /* 信息背景 */
```

### 功能色 (Functional Colors)

功能色用于特定的界面元素。

```css
/* 背景色 */
--color-background-primary: #ffffff;    /* 主背景 */
--color-background-secondary: #f8fafc;  /* 次背景 */
--color-background-tertiary: #f1f5f9;   /* 三级背景 */

/* 表面色 */
--color-surface-primary: #ffffff;      /* 主表面 */
--color-surface-secondary: #f8fafc;    /* 次表面 */
--color-surface-tertiary: #f1f5f9;     /* 三级表面 */

/* 边框色 */
--color-border-primary: #e2e8f0;       /* 主边框 */
--color-border-secondary: #cbd5e1;      /* 次边框 */
--color-border-tertiary: #94a3b8;      /* 三级边框 */

/* 文本色 */
--color-text-primary: #0f172a;         /* 主文本 */
--color-text-secondary: #475569;       /* 次文本 */
--color-text-tertiary: #64748b;        /* 三级文本 */
--color-text-disabled: #94a3b8;        /* 禁用文本 */
--color-text-inverse: #ffffff;         /* 反色文本 */
```

## 📏 间距系统

### 基础间距

基于 8px 网格系统，提供一致的间距规范。

```css
--spacing-xs: 4px;    /* 0.25rem */
--spacing-sm: 8px;    /* 0.5rem */
--spacing-md: 16px;   /* 1rem */
--spacing-lg: 24px;   /* 1.5rem */
--spacing-xl: 32px;   /* 2rem */
--spacing-2xl: 48px;  /* 3rem */
--spacing-3xl: 64px;  /* 4rem */
```

### 语义化间距

为不同的使用场景提供语义化的间距。

```css
/* 内边距 */
--padding-xs: 4px;    /* 小内边距 */
--padding-sm: 8px;    /* 小内边距 */
--padding-md: 16px;   /* 中等内边距 */
--padding-lg: 24px;   /* 大内边距 */
--padding-xl: 32px;   /* 超大内边距 */

/* 外边距 */
--margin-xs: 4px;     /* 小外边距 */
--margin-sm: 8px;     /* 小外边距 */
--margin-md: 16px;    /* 中等外边距 */
--margin-lg: 24px;    /* 大外边距 */
--margin-xl: 32px;    /* 超大外边距 */
```

## 🔲 圆角系统

提供统一的圆角规范，确保视觉一致性。

```css
--border-radius-sm: 2px;      /* 小圆角 */
--border-radius-md: 4px;      /* 中等圆角 */
--border-radius-lg: 8px;      /* 大圆角 */
--border-radius-xl: 12px;     /* 超大圆角 */
--border-radius-2xl: 16px;    /* 特大圆角 */
--border-radius-3xl: 24px;    /* 巨大圆角 */
--border-radius-full: 9999px; /* 完全圆角 */
```

**使用指南：**

- `sm` 用于小元素，如标签
- `md` 用于输入框和按钮
- `lg` 用于卡片和容器
- `xl` 用于模态框和抽屉
- `full` 用于圆形元素

## 🌫️ 阴影系统

提供层次感的阴影规范。

```css
--box-shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
--box-shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
--box-shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
--box-shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
--box-shadow-2xl: 0 25px 50px -12px rgb(0 0 0 / 0.25);
--box-shadow-inner: inset 0 2px 4px 0 rgb(0 0 0 / 0.05);
```

**使用指南：**

- `sm` 用于轻微提升
- `md` 用于卡片和按钮
- `lg` 用于模态框
- `xl` 用于下拉菜单
- `2xl` 用于重要提示

## 📝 字体系统

### 字体大小

提供一致的字体大小规范。

```css
--font-size-xs: 12px;     /* 小字体 */
--font-size-sm: 14px;     /* 小字体 */
--font-size-base: 16px;   /* 基础字体 */
--font-size-lg: 18px;     /* 大字体 */
--font-size-xl: 20px;     /* 超大字体 */
--font-size-2xl: 24px;    /* 标题字体 */
--font-size-3xl: 30px;    /* 大标题字体 */
--font-size-4xl: 36px;    /* 特大标题字体 */
--font-size-5xl: 48px;    /* 巨大标题字体 */
```

### 行高

提供合适的行高，确保文本可读性。

```css
--line-height-none: 1;        /* 无行高 */
--line-height-tight: 1.25;    /* 紧密行高 */
--line-height-snug: 1.375;    /* 舒适行高 */
--line-height-normal: 1.5;    /* 正常行高 */
--line-height-relaxed: 1.625; /* 宽松行高 */
--line-height-loose: 2;       /* 松散行高 */
```

### 字重

提供不同的字重选择。

```css
--font-weight-thin: 100;       /* 极细 */
--font-weight-extralight: 200; /* 超细 */
--font-weight-light: 300;     /* 细 */
--font-weight-normal: 400;    /* 正常 */
--font-weight-medium: 500;   /* 中等 */
--font-weight-semibold: 600; /* 半粗 */
--font-weight-bold: 700;     /* 粗 */
--font-weight-extrabold: 800; /* 超粗 */
--font-weight-black: 900;    /* 极粗 */
```

## 🎭 动画系统

### 持续时间

提供一致的动画持续时间。

```css
--duration-fast: 150ms;    /* 快速动画 */
--duration-normal: 300ms;  /* 正常动画 */
--duration-slow: 500ms;    /* 慢速动画 */
```

### 缓动函数

提供自然的动画缓动。

```css
--easing-linear: linear;
--easing-ease: ease;
--easing-ease-in: ease-in;
--easing-ease-out: ease-out;
--easing-ease-in-out: ease-in-out;
--easing-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
--easing-elastic: cubic-bezier(0.175, 0.885, 0.32, 1.275);
```

## 📱 响应式设计

### 断点系统

基于现代设备的断点设计。

```css
/* 断点 */
xs: 0px;      /* 手机 */
sm: 640px;    /* 大手机 */
md: 768px;    /* 平板 */
lg: 1024px;   /* 小桌面 */
xl: 1280px;   /* 桌面 */
2xl: 1536px;  /* 大桌面 */
```

### 网格系统

提供灵活的网格布局。

```css
/* 网格列数 */
xs: 4列;      /* 手机 */
sm: 6列;      /* 大手机 */
md: 8列;      /* 平板 */
lg: 12列;     /* 桌面 */
xl: 12列;     /* 大桌面 */
2xl: 12列;    /* 超大桌面 */
```

## 🌙 主题系统

### 浅色主题

默认的浅色主题，适合日间使用。

### 深色主题

深色主题，适合夜间使用，减少眼部疲劳。

### 系统主题

自动跟随系统主题设置。

## 🧩 组件系统

### 基础组件

- **Button**: 按钮组件
- **Input**: 输入框组件
- **Card**: 卡片组件
- **Modal**: 模态框组件
- **Tooltip**: 提示组件

### 布局组件

- **PageLayout**: 页面布局
- **ContentLayout**: 内容布局
- **GridLayout**: 网格布局
- **FlexLayout**: 弹性布局

### 认证组件

- **AuthLayout**: 认证页面布局
- **AuthCard**: 认证卡片
- **AuthForm**: 认证表单
- **AuthField**: 认证字段
- **AuthButton**: 认证按钮

### 交互组件

- **LoadingState**: 加载状态
- **ErrorState**: 错误状态
- **EmptyState**: 空状态
- **Notification**: 通知消息
- **FormValidation**: 表单验证
- **Skeleton**: 骨架屏

## 🎯 使用指南

### 1. 颜色使用

- 优先使用语义化颜色
- 确保足够的对比度
- 避免过度使用颜色

### 2. 间距使用

- 使用 8px 网格系统
- 保持一致的间距
- 使用语义化间距

### 3. 字体使用

- 选择合适的字体大小
- 保持合适的行高
- 使用合适的字重

### 4. 组件使用

- 优先使用设计系统组件
- 保持组件的一致性
- 遵循组件的使用规范

## 🔧 开发指南

### 1. 安装依赖

```bash
npm install @itsm/design-system
```

### 2. 导入组件

```tsx
import { Button, Input, Card } from '@itsm/design-system';
```

### 3. 使用主题

```tsx
import { ThemeProvider } from '@itsm/design-system';

<ThemeProvider>
  <App />
</ThemeProvider>
```

### 4. 自定义主题

```tsx
import { ThemeProvider } from '@itsm/design-system';

const customTheme = {
  colors: {
    primary: {
      500: '#your-color',
    },
  },
};

<ThemeProvider theme={customTheme}>
  <App />
</ThemeProvider>
```

## 📊 最佳实践

### 1. 设计原则

- 保持简洁明了
- 提供清晰的视觉层次
- 确保一致性

### 2. 开发原则

- 使用 TypeScript
- 编写测试
- 遵循代码规范

### 3. 维护原则

- 定期更新文档
- 收集用户反馈
- 持续改进

## 🚀 未来规划

### 短期目标

- 完善组件库
- 优化性能
- 增加测试覆盖

### 中期目标

- 支持更多主题
- 增加动画效果
- 优化无障碍访问

### 长期目标

- 支持多语言
- 增加设计工具
- 建立设计社区

## 📞 支持

如果您在使用设计系统时遇到问题，请：

1. 查看文档和示例
2. 搜索已知问题
3. 提交 Issue
4. 联系开发团队

---

**设计系统版本**: 1.0.0  
**最后更新**: 2024年12月  
**维护团队**: ITSM 开发团队
