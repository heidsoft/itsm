# ITSM产品测试质量提升计划

> **日期**: 2026-02-10  
> **目标**: 提升测试覆盖率从4.21%到60%+，修复所有失败测试  
> **当前状态**: 10个测试套件失败，224个测试通过

---

## 📊 当前测试状态

### 测试统计
- **测试套件**: 18个（10个失败，8个通过）
- **测试用例**: 254个（30个失败，224个通过）
- **代码覆盖率**: 4.21%
  - Statements: 4.21% (428/10146)
  - Branches: 4.3% (159/3693)
  - Functions: 2.53% (86/3388)
  - Lines: 4.26% (391/9169)

### 失败的测试套件

1. ✅ **cmdb-page.test.tsx** - ReferenceError: modalConfirmSpy初始化问题
2. ✅ **UnifiedFilters.test.tsx** - dayjs mock配置问题
3. ⚠️ **UnifiedTable.test.tsx** - 组件props不匹配
4. ⚠️ **tickets-page.test.tsx** - 部分测试失败
5. ⚠️ **dashboard-page.test.tsx** - 渲染测试失败
6. ⚠️ **login/page.test.tsx** - 表单结构测试失败
7. ⚠️ **formatters.test.ts** - 部分测试失败
8. ⚠️ **ApprovalChainStats.test.tsx** - 统计卡片测试失败
9. ⚠️ **validation.test.ts** - 部分测试失败
10. ⚠️ **TicketFilters.test.tsx** - 部分测试失败

---

## 🎯 提升计划

### 阶段1: 修复失败测试（今天，2小时）

#### 1.1 修复cmdb-page.test.tsx ⭐⭐⭐⭐⭐
**问题**: modalConfirmSpy在初始化前被访问

**解决方案**:
```typescript
// 将modalConfirmSpy声明移到jest.mock之前
let modalConfirmSpy: jest.Mock;

jest.mock('antd', () => {
  // 在这里初始化
  modalConfirmSpy = jest.fn((config: { onOk?: () => void }) => {
    config?.onOk?.();
  });
  // ...
});
```

**优先级**: P0（阻塞测试运行）

---

#### 1.2 修复UnifiedFilters.test.tsx ⭐⭐⭐⭐
**问题**: dayjs mock不完整

**解决方案**:
```typescript
jest.mock('dayjs', () => {
  const mockDayjs = (date?: any) => {
    const d = date ? new Date(date) : new Date();
    return {
      format: (fmt: string) => d.toISOString(),
      toDate: () => d,
      isValid: () => true,
      isBefore: () => false,
      isAfter: () => false,
      isSame: () => false,
    };
  };
  mockDayjs.extend = jest.fn();
  return mockDayjs;
});
```

**优先级**: P0

---

#### 1.3 修复其他失败测试 ⭐⭐⭐
- UnifiedTable.test.tsx: 更新组件props
- tickets-page.test.tsx: 修复Alert组件警告
- dashboard-page.test.tsx: 修复数据mock
- login/page.test.tsx: 更新表单结构断言

**优先级**: P1

---

### 阶段2: 提升核心模块测试覆盖率（本周，2天）

#### 2.1 工单管理模块（目标80%）⭐⭐⭐⭐⭐

**需要测试的组件**:
- TicketList.tsx
- TicketDetail.tsx
- TicketForm.tsx
- TicketKanban.tsx
- TicketSubtasks.tsx

**测试重点**:
- 工单列表渲染和筛选
- 工单详情展示
- 工单创建和编辑
- 工单状态流转
- 子任务管理

---

#### 2.2 Dashboard模块（目标70%）⭐⭐⭐⭐⭐

**需要测试的组件**:
- DashboardOverview.tsx
- SystemAlertCard.tsx
- TicketListCard.tsx
- SLAViolationMonitor.tsx
- PerformanceMonitor.tsx

**测试重点**:
- KPI指标展示
- 实时数据更新
- 图表渲染
- 告警展示

---

#### 2.3 用户管理模块（目标70%）⭐⭐⭐⭐

**需要测试的组件**:
- UserList.tsx
- UserForm.tsx
- UserProfile.tsx

**测试重点**:
- 用户列表CRUD
- 权限管理
- 用户信息编辑

---

#### 2.4 认证模块（目标80%）⭐⭐⭐⭐⭐

**需要测试的组件**:
- AuthGuard.tsx
- AuthField.tsx
- AuthForm.tsx
- LoginPage

**测试重点**:
- 登录流程
- 权限验证
- 路由守卫
- Token管理

---

### 阶段3: API和工具函数测试（本周，1天）

#### 3.1 API客户端测试（目标90%）⭐⭐⭐⭐⭐

**需要测试的文件**:
- lib/api/client.ts
- lib/api/tickets.ts
- lib/api/users.ts
- lib/api/dashboard.ts

**测试重点**:
- HTTP请求正确性
- 错误处理
- 响应数据转换
- 请求拦截器

---

#### 3.2 工具函数测试（目标95%）⭐⭐⭐⭐⭐

**需要测试的文件**:
- lib/utils/formatters.ts
- lib/utils/validation.ts
- lib/utils/date-utils.ts
- lib/utils/error-handler.ts

**测试重点**:
- 数据格式化
- 表单验证
- 日期处理
- 错误处理

---

#### 3.3 Hooks测试（目标80%）⭐⭐⭐⭐

**需要测试的文件**:
- hooks/useDashboardData.ts
- hooks/useTickets.ts
- hooks/useAuth.ts
- hooks/useNotifications.ts

**测试重点**:
- 数据获取
- 状态管理
- 副作用处理
- 错误处理

---

### 阶段4: 集成测试（下周，2天）

#### 4.1 关键流程测试 ⭐⭐⭐⭐⭐

**测试场景**:
1. 用户登录 → 查看Dashboard → 创建工单
2. 工单分配 → 状态更新 → 添加评论
3. SLA监控 → 告警触发 → 通知发送
4. 变更管理 → 审批流程 → 执行回滚

---

#### 4.2 错误场景测试 ⭐⭐⭐⭐

**测试场景**:
1. 网络错误处理
2. 权限不足处理
3. 数据验证失败
4. 并发操作冲突

---

### 阶段5: E2E测试（下周，1天）

#### 5.1 核心用户流程 ⭐⭐⭐⭐⭐

**使用Playwright/Cypress**:
1. 完整的工单生命周期
2. 用户管理流程
3. Dashboard数据展示
4. 搜索和筛选功能

---

## 📈 预期成果

### 覆盖率目标

| 指标 | 当前 | 目标 | 提升 |
|------|------|------|------|
| **Statements** | 4.21% | 60% | +55.79% |
| **Branches** | 4.3% | 55% | +50.7% |
| **Functions** | 2.53% | 65% | +62.47% |
| **Lines** | 4.26% | 60% | +55.74% |

### 质量指标

| 指标 | 当前 | 目标 |
|------|------|------|
| **测试通过率** | 88% | 100% |
| **测试套件通过率** | 44% | 100% |
| **平均测试时间** | 28.5s | <20s |
| **测试稳定性** | 中 | 高 |

---

## 🛠️ 实施策略

### 1. 测试优先级

**P0 - 立即修复**:
- 所有失败的测试
- 阻塞CI/CD的问题

**P1 - 本周完成**:
- 核心业务模块测试
- API和工具函数测试

**P2 - 下周完成**:
- 集成测试
- E2E测试

**P3 - 后续优化**:
- 性能测试
- 压力测试
- 安全测试

---

### 2. 测试最佳实践

#### 2.1 单元测试原则
- ✅ 测试单一职责
- ✅ 使用有意义的测试名称
- ✅ 遵循AAA模式（Arrange-Act-Assert）
- ✅ Mock外部依赖
- ✅ 避免测试实现细节

#### 2.2 组件测试原则
- ✅ 测试用户交互
- ✅ 测试可访问性
- ✅ 测试错误状态
- ✅ 测试加载状态
- ✅ 使用Testing Library最佳实践

#### 2.3 集成测试原则
- ✅ 测试真实用户场景
- ✅ 最小化Mock
- ✅ 测试数据流
- ✅ 测试错误恢复

---

### 3. 测试工具配置

#### 3.1 Jest配置优化
```javascript
{
  "collectCoverageFrom": [
    "src/**/*.{ts,tsx}",
    "!src/**/*.d.ts",
    "!src/**/*.stories.tsx",
    "!src/**/__tests__/**"
  ],
  "coverageThresholds": {
    "global": {
      "statements": 60,
      "branches": 55,
      "functions": 65,
      "lines": 60
    }
  }
}
```

#### 3.2 Testing Library配置
```typescript
// jest.setup.ts
import '@testing-library/jest-dom';
import { configure } from '@testing-library/react';

configure({
  testIdAttribute: 'data-testid',
  asyncUtilTimeout: 3000,
});
```

---

## 📝 测试模板

### 单元测试模板
```typescript
describe('ComponentName', () => {
  // Setup
  beforeEach(() => {
    jest.clearAllMocks();
  });

  // Happy path
  it('should render correctly with valid props', () => {
    // Arrange
    const props = { /* ... */ };
    
    // Act
    render(<ComponentName {...props} />);
    
    // Assert
    expect(screen.getByText('Expected Text')).toBeInTheDocument();
  });

  // Error cases
  it('should handle errors gracefully', () => {
    // ...
  });

  // Edge cases
  it('should handle empty data', () => {
    // ...
  });
});
```

### 集成测试模板
```typescript
describe('Feature Flow', () => {
  it('should complete user journey successfully', async () => {
    // Setup
    const user = userEvent.setup();
    
    // Navigate
    render(<App />);
    
    // Interact
    await user.click(screen.getByRole('button', { name: 'Login' }));
    
    // Verify
    await waitFor(() => {
      expect(screen.getByText('Dashboard')).toBeInTheDocument();
    });
  });
});
```

---

## 🎯 成功标准

### 必须达成
- ✅ 所有测试通过（100%通过率）
- ✅ 代码覆盖率 > 60%
- ✅ 核心模块覆盖率 > 80%
- ✅ 测试运行时间 < 30秒

### 期望达成
- ✅ 集成测试覆盖关键流程
- ✅ E2E测试覆盖核心场景
- ✅ 测试文档完善
- ✅ CI/CD集成

### 可选达成
- ✅ 性能测试基准
- ✅ 可访问性测试
- ✅ 视觉回归测试

---

## 📅 时间表

| 阶段 | 任务 | 时间 | 负责人 |
|------|------|------|--------|
| **Day 1** | 修复失败测试 | 2小时 | Dev |
| **Day 2-3** | 核心模块测试 | 2天 | Dev |
| **Day 4** | API和工具测试 | 1天 | Dev |
| **Day 5-6** | 集成测试 | 2天 | Dev |
| **Day 7** | E2E测试 | 1天 | Dev |
| **Day 8** | 优化和文档 | 1天 | Dev |

**总计**: 8个工作日

---

## 🚀 立即行动

### 今天的任务（2小时）

1. ✅ 修复cmdb-page.test.tsx的modalConfirmSpy问题
2. ✅ 修复UnifiedFilters.test.tsx的dayjs mock
3. ✅ 修复UnifiedTable.test.tsx的props问题
4. ✅ 运行测试确保全部通过
5. ✅ 提交代码并更新文档

---

**报告生成时间**: 2026-02-10  
**下次更新**: 每日进度更新
